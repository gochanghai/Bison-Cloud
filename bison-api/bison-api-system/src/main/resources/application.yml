server:
  port: 9501
spring:
  profiles:
    active: dev
  application:
    name: bison-oauth2-api
  cloud:
    nacos:
      discovery:
        server-addr: localhost.nacos:8848
management:
  endpoints:
    web:
      exposure:
        include: "*"
